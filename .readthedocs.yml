version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  jobs:
    post_install:
      - python -m pip install --upgrade --no-cache-dir ".[docs]"
sphinx:
  configuration: docs/source/conf.py
